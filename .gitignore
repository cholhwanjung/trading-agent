# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# uv / venv
.venv/

# 자격증명 · 환경변수 (절대 커밋 금지)
.env
.env.*
!.env.example
*.key
secrets/

# 메모리 store · 로그 · 데이터 (data/ 는 런타임 산출물)
data/
*.sqlite
*.sqlite-*
*.db
logs/
*.log

# OS / 에디터
.DS_Store
.idea/
.vscode/

# 로컬 전용 — 개인 문서·노트 (어플리케이션 비필수)
.claude/
CLAUDE.md
docs/
trading-agent-design.md
commercial-trading-agents-research.md

# 테스트
tests/
.pytest_cache/

# 개인 전략 — 투자 철학 플레이북. 프롬프트 블록 중 이것만 비공개다.
# 나머지 블록은 역할·제약·출력 스키마 같은 기계적 조립 규칙이라 공개해도 무방하다
# (블록으로 갈리기 전에도 그 부분은 코드 상수라 추적 대상이었다 — 공개 범위는 그대로).
trader/prompts/blocks/playbook.md

# 기계 종속 운영 설정 — 개인 절대경로 포함 (이식 스케줄링은 scripts/scheduler.py + Docker)
scripts/launchd/
